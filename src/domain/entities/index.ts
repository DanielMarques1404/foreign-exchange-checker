export type LiveRatesType = {
  currency: string;
  value: number;
  change?: number;
};


export type TabButtonType = {
  id: number;
  label: string;
  counter?: number;
};

export type SelectItemType = {
  id: number;
  label: string;
  counter?: number;
};
